= render 'navigation', step: @step

%h2.number 질문 #{@step}.
%h2 귀하의 연령은 어떻게 되십니까?

= form_tag 'next_step', remote: true do
  = render 'question', question: :age
  .row.btns
    .col-xs-6
      = submit_tag '< 이전으로', class: 'btn-back btn-block', name: 'back'
    .col-xs-6
      = submit_tag '다음으로 >', class: 'btn-next btn-block', disabled: params.dig(:answers, :age).blank?, id: 'step-submit'
